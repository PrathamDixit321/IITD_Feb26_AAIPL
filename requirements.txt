# Core ML frameworks
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0

# Training utilities
peft>=0.7.0
trl>=0.7.0
bitsandbytes>=0.41.0

# AMD ROCm support (for MI300 GPU)
# Install ROCm-compatible PyTorch from: https://pytorch.org/get-started/locally/
# pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm5.7

# Unsloth for faster inference (optional, for Llama models)
# unsloth @ git+https://github.com/unslothai/unsloth.git

# Utilities
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Progress bars and logging
tqdm>=4.65.0
tensorboard>=2.14.0
wandb>=0.15.0  # Optional: for experiment tracking

# JSON and data handling
jsonlines>=3.1.0

# Evaluation metrics
evaluate>=0.4.0
rouge-score>=0.1.2
nltk>=3.8.0

# Development tools
jupyter>=1.0.0
ipython>=8.12.0
black>=23.0.0
flake8>=6.0.0
